---
description: Create API tokens to access the platform programmatically.
---

# Create API tokens

Account administrators can create API tokens for authentication directly from the platform.

### Creating a new API token

To create a new token:

1. Go to **Settings** > **API tokens**.&#x20;
2. Select **Add** to start the **Add API token** wizard.

<div data-with-frame="true"><figure><img src="../../../.gitbook/assets/image (990).png" alt=""><figcaption><p>The Add API token wizard in the platform.</p></figcaption></figure></div>

3. In the **General** step, provide the following details:
   1. **Logo** - (Optional) Add an image for your token. You can select a file to upload or drop your file into the selection area. By uploading an image, you can easily identify your token from the list of tokens.&#x20;
   2. **Name** - (Required) Enter a name for your token.&#x20;
   3. **Description** - (Optional) Add the token description.
4. Select **Next**. In the **Modules** step, select the platform modules that the token allows access to. When done, select **Next**. Note that only the modules that SoftwareOne has enabled for your account are displayed on the page.&#x20;

<div data-with-frame="true"><figure><img src="../../../.gitbook/assets/image (991).png" alt=""><figcaption><p>The Module selection step when adding an API token.</p></figcaption></figure></div>

5. In the **Overview** step, verify the details and select **Add**. The token is created, and the token summary is displayed.
6. Select **View details** to open the details page of your newly created token. Otherwise, select **Close**.&#x20;
